name: Fix Snooker player Phase 1 root state

on:
  push:
    branches:
      - agent/snooker-player-phase1-inline-v2

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply root-owned directory state fix
        run: node scripts/fix-player-phase1-root-state.mjs
      - name: Commit scoped fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "fix: preserve player directory state in root controller"
          git push origin HEAD:agent/snooker-player-phase1-inline-v2
