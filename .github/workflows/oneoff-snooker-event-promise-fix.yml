name: One-off Snooker event promise fix

on:
  push:
    branches:
      - agent/snooker-dashboard-resilience-core
    paths:
      - scripts/fix-snooker-event-promise.mjs
      - .github/workflows/oneoff-snooker-event-promise-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/snooker-dashboard-resilience-core
          fetch-depth: 0
      - run: node scripts/fix-snooker-event-promise.mjs
      - run: rm scripts/fix-snooker-event-promise.mjs .github/workflows/oneoff-snooker-event-promise-fix.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/snooker/snooker-data-center-v2.tsx scripts/fix-snooker-event-promise.mjs .github/workflows/oneoff-snooker-event-promise-fix.yml
          git commit -m "fix: type event detail inflight promise"
          git push origin HEAD:agent/snooker-dashboard-resilience-core
